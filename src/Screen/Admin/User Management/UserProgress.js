import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const UserProgress = () => {
  return (
    <View>
      <Text>UserProgress</Text>
    </View>
  )
}

export default UserProgress

const styles = StyleSheet.create({})